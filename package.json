{
  "name": "blockchain-student-attendance",
  "version": "1.0.0",
  "private": true,
  "description": "Blockchain-based student attendance system with Solidity and a web frontend",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "dotenv": "^16.6.1",
    "ethers": "^6.14.0",
    "hardhat": "^2.28.0"
  }
}
